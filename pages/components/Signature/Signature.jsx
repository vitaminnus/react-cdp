import React from 'react';
import styles from './Signature.scss';

const Signature = () => (
  <footer className={styles.footer}>
    <p className={styles.name}>netflixroulette</p>
  </footer>
);

export default Signature;
