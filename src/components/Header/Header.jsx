import React from 'react';
import styles from './Header.scss';

const Header = () => (
  <header className={styles.header}>
    <p className={styles.logo}>netflixroulette</p>
  </header>
);

export default Header;
